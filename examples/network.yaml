apiVersion: gcp.platformref.crossplane.io/v1alpha1
kind: Network
metadata:
  name: platform-ref-gcp-network
spec:
  id: platform-ref-gcp-network
  clusterRef:
    id: platform-ref-gcp-cluster
  compositionSelector:
    matchLabels:
      provider: GCP
