apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: ${KUBECTL} apply -f ${PWD}/examples/provider-default-gcp.yaml
  - command: ${KUBECTL} -n upbound-system apply -f ${PWD}/examples/cluster-claim.yaml